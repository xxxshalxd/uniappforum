<mapper namespace="com.lxd.forumbackend.communicate.mapper.postMapper">
    <select id="selectByUserId" resultType="postEntity">
        SELECT * FROM posts WHERE user_id = #{userId}
    </select>

</mapper>